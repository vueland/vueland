---
'@vueland/ui': patch
---

fix: `CMenu` with `strategy="reverse"` no longer overflows the viewport when the content doesn't fit on either side — the reversed position is now clamped to the screen edge
